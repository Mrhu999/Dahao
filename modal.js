const modal = document.getElementById("modal");  // 获取 modal 元素

function openModal() {
  modal.style.display = "flex"; // 显示 modal
}

function closeModal() {
  modal.style.display = "none"; // 关闭 modal
}
